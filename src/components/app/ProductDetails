import React, { useState} from "react";
import * as firebase from 'firebase'
import { Container, Button } from "react-bootstrap";
import { withStdTopNav, withStdBottomNav } from "../HOC/NavHOC";
import { db } from "../../firebase";
import { Card, Row, Col, Image } from "react-bootstrap";
import { firstLetterToUpperCase } from "../../utils/utils";


export const ProductDetails = withStdBottomNav(withStdTopNav(() => {
    
    const [name,setName] = useState("Lemon");
    const [details,setDetails] = useState("The lemon, Citrus limon (L.) Osbeck, is a species of small evergreen tree in the flowering plant family");
    const [price,setPrice] = useState(399);
    const [place,setPlace] =useState("India");
    const [company, setCompany] = useState("Badass Company")
    const [unit, setUnit] =useState("KG");
    const [image,setImage] = useState("https://firebasestorage.googleapis.com/v0/b/shopdeliverymanagement.appspot.com/o/Product%20Images%2FLemon.jpg?alt=media&token=a5fa505c-a5be-40dc-9159-56f52f28fe64");
    const [loading,setLoading] = useState(true);

    return (
        <Container>
            <Card style={{ padding: "20px 20px", margin: "10px 10px" }}>
            <Row>
                <Col xs={3} className="row align-items-center">
                <h1>{firstLetterToUpperCase(name)}</h1>
                    <span>
                    <Image src={image} width="100%" alt="Error" />
                    </span>
                </Col>
            </Row>
            <Row>
                <Col xs={4}>
                    <span>
                        <p className="font-weight-bold">From {place} | {company}</p>
                        <p className="font-weight-bold">Rs. {price} per {unit}</p>
                        <Button>Add to Cart</Button>
                        <p></p>
                        <p>{details}</p>
                    </span>
                </Col>
            </Row>
        </Card>            
        </Container>
    );

}));
